# Docker Compose reads this file when copied to .env.
# Requested local/test PIN; choose a different four-digit PIN for public use.
ADMIN_PIN=1234
PORT=3000
# For HTTPS behind a reverse proxy, set both of these:
COOKIE_SECURE=false
PUBLIC_ORIGIN=
